# Example remote state — copy to backend.tf and fill in after creating the S3 bucket.
#
# aws s3 mb s3://relaydesk-terraform-state-bucket --region ap-south-1
# aws s3api put-bucket-versioning --bucket relaydesk-terraform-state-bucket --versioning-configuration Status=Enabled --region ap-south-1
#
# terraform {
#   backend "s3" {
#     bucket       = "relaydesk-terraform-state-bucket"
#     key          = "relaydesk/prod/terraform.tfstate"
#     region       = "ap-south-1"
#     encrypt      = true
#     use_lockfile = true
#   }
# }
