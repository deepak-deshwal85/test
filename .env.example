# Root environment template

Copy settings into each project:

- `voice-agent/.env` — LiveKit, voice providers, RAG client URL
- `api/.env` — OpenAI, Qdrant, RAG API server

See [README.md](README.md) for setup steps.
